{
  "name": "aplikasipos",
  "version": "1.0.0",
  "private": true,
  "description": "Aplikasi kasir POS berbasis web yang siap dikemas ke Android dengan Capacitor.",
  "scripts": {
    "build": "echo \"Proyek ini menggunakan file HTML/JS langsung, tidak ada build langkah khusus.\"",
    "sync": "npx cap sync android",
    "open:android": "npx cap open android"
  },
  "devDependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/cli": "^5.0.0"
  }
}
